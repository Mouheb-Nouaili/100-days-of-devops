# Day 004 : Script Execution Permissions
it was asked to add the permission of execution to a scrit.sh in server2 

Connect with the named user and become root:

```bash
ssh steve@stapp02 

sudo su -
 
```
Acced to the ffolder and change the permissions to add the execution one 

```bash
cd /tmp

ls -l

chmod 755 xfusioncrop.sh

ls -l 
```
